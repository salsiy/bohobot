theme = "blowfish"
baseURL = "https://bohobot.com"
defaultContentLanguage = "en"
buildFuture = true
buildDrafts = false

[taxonomies]
  tag = "tags"
  category = "categories"
  author = "authors"
  series = "series"

# Required for Blowfish client-side search (generates /index.json)
[outputs]
  home = ["HTML", "RSS", "JSON"]